export class Crypt {}

Crypt.prototype.encrypt = function (value) {
    let encryptedValue;
    // add encrypt algorithm and encrypt input value
    return encryptedValue;
};

Crypt.prototype.decrypt = function (value) {
    let decryptedValue;
    // add decrypt algorithm and decrypt input value
    return decryptedValue;
};
