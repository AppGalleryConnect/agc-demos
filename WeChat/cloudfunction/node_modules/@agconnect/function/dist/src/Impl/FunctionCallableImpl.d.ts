import { FunctionCallable, FunctionResult } from '@agconnect/function-types';
import { FunctionBackend } from "../server/FunctionBackend";
export declare class FunctionCallableImpl implements FunctionCallable {
    private httpTriggerURI;
    timeout: number | undefined;
    functionBackend: FunctionBackend;
    constructor(httpTriggerURI: string);
    call(reqBody?: any): Promise<FunctionResult>;
    clone(timeout: number): FunctionCallable;
}
